import pandas as pd
import numpy as np

# Set seed for reproducibility
np.random.seed(42)
n_customers = 1000

# 1. GENERATE CUSTOMER RFM DATA
data = {
    'CustomerID': [f'CUST_{i:04d}' for i in range(1, n_customers + 1)],
    'Recency': np.random.randint(1, 365, size=n_customers),      # Days since last order
    'Frequency': np.random.randint(1, 50, size=n_customers),     # Total purchases
    'Monetary': np.random.uniform(10, 5000, size=n_customers).round(2)  # Total spending ($)
}

df = pd.DataFrame(data)

# 2. CHURN LOGIC & RISK CLASSIFICATION
# Rule: Recency > 180 days and Frequency < 10 purchases = Churned / High Risk
df['Churn_Status'] = np.where((df['Recency'] > 180) & (df['Frequency'] < 10), 1, 0)
df['Risk_Level'] = np.where(df['Churn_Status'] == 1, 'High Risk', 'Low Risk')

# 3. EXPORT TO CSV
file_name = "Predicted_Customer_Churn_Results.csv"
df.to_csv(file_name, index=False)

print(f"Dataset successfully created and exported to '{file_name}'!")